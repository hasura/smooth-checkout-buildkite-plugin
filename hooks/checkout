#!/bin/bash

set -euo pipefail

# echo "BUILDKITE_PLUGIN_SMOOTH_GIT_MODE is $BUILDKITE_PLUGIN_SMOOTH_GIT_MODE"
echo "BUILDKITE_PLUGIN_SMOOTH_GIT_SKIP_CHECKOUT is $BUILDKITE_PLUGIN_SMOOTH_GIT_SKIP_CHECKOUT"